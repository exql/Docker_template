version: "3.8"
   
services:
  db:
    image: mysql:5.7
    environment:
      MYSQL_USER: virtualUser
      MYSQL_PASSWORD: virtualPass
      MYSQL_DATABASE: virtual_assistanst
      MYSQL_ROOT: Mysql_root
      MYSQL_ROOT_PASSWORD: VirtualPass
    restart:
      unless-stopped
    volumes:
      - ./db_data:/var/lib/mysql
      - ./initial.sql:/docker-entrypoint-initdb.d/initial.sql:ro
    ports:
      - "3306:3306"
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db